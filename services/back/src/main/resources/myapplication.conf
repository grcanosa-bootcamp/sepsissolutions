sepsis {
  datadir: "/mnt/DATAEXT/DATAHACK/CasoDeUso/sepsissolutions/volumes/data/"
  datadir: ${?DATADIR}
  file {
    end: "csv"
    begin: "prueba"
  }
}

cassandra{
  host: "localhost"
  host: ${?CASSANDRAHOST}
  connection_timeout: 120
  connection_timeout: ${?CASSANDRA_CONNECTION_TIMEOUT}
}

http {
  host: "0.0.0.0"
  port: "7777"
  port: ${?RESTAPIPORT}
}